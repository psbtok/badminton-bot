version: '3.8'

services:
  admin:
    build: .
    working_dir: /app
    command: ["python", "admin.py"]
    restart: always
    env_file:
      - .env
    volumes:
      - ./data:/app/data

  user:
    build: .
    working_dir: /app
    command: ["python", "user.py"]
    restart: always
    env_file:
      - .env
    volumes:
      - ./data:/app/data